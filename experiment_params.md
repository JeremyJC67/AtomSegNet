# Atom Cluster Experiment Parameters

| Stage | Scope | Key Parameters | Notes |
|-------|-------|----------------|-------|
| Blur Generation (`blur_dataset.py`) | All 1 175 images | `sigma=14`, `--normalize`, pattern `*.jpg`, `--head 0 --tail 0` | Outputs `blured_1742_heavier/*_outline.png` |
| Cluster Segmentation (`atom_cluster_segment.py`) | All 1 175 blurred images | `--sigma-bg 0`, `--sigma-fg 2.0`, `--use-local`, `--block-size 151`, `--offset 0.01`, `--min-area 3000`, `--close 10`, `--open 2`, `--keep-k 2`, `--smooth-k 7`, `--simplify-eps 3.0`, `--dark-object`, `--save-crops` | Writes masks / overlays / boundary coords to `atom_cluster_out/` |
| Point Filtering & Overlay (`apply_mask_to_points.py`) – baseline | All 1 175 folders processed earlier | `--dilate 1`, `--radius 3`, `--color red`, suffix `gaussian_blur` | Produces filtered txt + red-point overlays per folder |
| Point Filtering & Overlay (`apply_mask_to_points.py`) – expanded band | First 50 folders (IDs `0000`–`0049`) | `--dilate 15`, `--radius 3`, `--color red`, suffix `gaussian_blur`, `--head 50 --tail 0` | Keeps extra ~15 px margin; recent run for comparison |
| Point Dedup (gaussian blur clean) | First 50 folders (IDs 0000–0049) | clean_atom_points: score_threshold=0.0, nn_factor=0.45, min_dist=0; overlay: dataset image overwrite, radius=3, color=red | Overwrites *_origin_gaussian_blur_Gen1-gaussianMask.png with deduped coordinates |
| point_postprocess: pixel-grid + NN dedup | First 50 folders (IDs 0000–0049) | clean_atom_points: pixel_cell=1.0, score_threshold=0.0, nn_factor=0.95, min_dist=12 | Overwrites *_origin_gaussian_blur_Gen1-gaussianMask.png with deduped coordinates |
| overlay comparison (orig vs clean) | First 50 folders (IDs 0000–0049) | original overlay: *_pos_gaussian_blur_*.txt; cleaned overlay: *_pos_gaussian_blur_..._clean.txt | Saved *_origin_gaussian_blur_Gen1-gaussianMask.png (orig) and *_origin_gaussian_blur_cleaned_Gen1-gaussianMask.png (clean) |
| point_postprocess: pixel-cell merge + NN dedup (10px) | First 50 folders (IDs 0000–0049) | clean_atom_points: pixel_cell=1.0, score_threshold=0.0, nn_factor=0.8, min_dist=10 | Overlays saved as *_origin_gaussian_blur_original_Gen1-gaussianMask.png and *_origin_gaussian_blur_cleaned_Gen1-gaussianMask.png |
| enhanced denoise generation | head=50, tail=50 | sigma_bg=18, sigma_fg=1.8, clahe=True | Outputs atomsegnet_enhanced_denoise/XXXX.png |
| enhanced inference | head/tail images | batch_run_atomsegnet.py input=atomsegnet_enhanced_denoise output=atomsegnet_enhanced_results --cuda --overwrite | Produces standard AtomSegNet outputs for enhanced images |
| enhanced postprocess | all enhanced results | clean_atom_points: pixel_cell=1.0, nn_factor=0.8, min_dist=10, quantile=0.65, min_score=0.4 | Score thresholds ≈0.807–0.845, dedup radii 11.8–13.4 px; outputs *_pos_Gen1-gaussianMask_gaussian_blur_clean.txt + *_origin_enhanced_{original,cleaned} |
| enhanced cluster filtering | head=50, tail=50 | apply_cluster_filter.py: mask dilation=1, draw radius=3, color=red, suffix=gaussian_blur | Filters cleaned points with atom_cluster_out masks; overwrites *_origin_gaussian_blur_Gen1-gaussianMask.png and *_pos_gaussian_blur_Gen1-gaussianMask.txt |
| enhanced cluster filtering ( +15 px margin ) | head=50, tail=50 | apply_cluster_filter.py: mask dilation=15, draw radius=3, color=red, suffix=gaussian_blur_d15 | Saves expanded-footprint overlays *_origin_gaussian_blur_d15_Gen1-gaussianMask.png and coordinates *_pos_gaussian_blur_d15_Gen1-gaussianMask.txt |
| enhanced mask filter rerun (sigma14) | head=50, tail=50 | apply_cluster_filter.py: mask_root=atom_cluster_out_sigma14, mask_template=`{}_heavyblur_mask.png`, detection_root=atomsegnet_enhanced_results, dir_template=`{}`, points_template=`{}_pos_Gen1-gaussianMask.txt`, dilate=15, radius=3, color=red, suffix=gaussian_blur | Overwrites *_origin_gaussian_blur_Gen1-gaussianMask.png / *_pos_gaussian_blur_Gen1-gaussianMask.txt for enhanced results |
| enhanced dedup rerun | head=50, tail=50 | clean_atom_points on atomsegnet_enhanced_results, pattern `????_pos_gaussian_blur_Gen1-gaussianMask.txt`, pixel_cell=1.0, nn_factor=0.8, min_dist=10, quantile=0.65, min_score=0.4; overlay_clean_points radius=3 color=red base=/dataset/1742 | Regenerates *_pos_Gen1-gaussianMask_gaussian_blur_clean.txt & *_origin_clean_Gen1-gaussianMask.png for enhanced results |
| sigma14 blur regeneration | All 1 175 images | blur_dataset.py: `--sigma 14 --normalize --pattern '*.jpg' --head 0 --tail 0`; output dir `blured_1742_sigma14` | Basis for new contour extraction run |
| sigma14 cluster extraction | All 1 175 sigma14 images | atom_cluster_segment.py on `blured_1742_sigma14`: `--sigma-bg 0 --sigma-fg 2.0 --use-local --block-size 151 --offset 0.01 --min-area 3000 --close 10 --open 2 --keep-k 2 --smooth-k 7 --simplify-eps 3.0 --dark-object --save-crops`; output `atom_cluster_out_sigma14` | Generates `*_heavyblur_mask.png` masks + diagnostics |
| denoise mask filter (dilate 15) | head=50, tail=50 | apply_cluster_filter.py: mask_root=atom_cluster_out_sigma14, mask_template=`{}_heavyblur_mask.png`, detection_root=atomsegnet1742_results, dir_template=`{}_denoise`, points_template=`{}_denoise_pos_Gen1-gaussianMask.txt`, suffix=gaussian_blur, dilate=15, radius=3, color=red | Produces `_pos_gaussian_blur_Gen1-gaussianMask.txt` and `_origin_gaussian_blur_Gen1-gaussianMask.png` using raw detections |
| denoise mask-filtered dedup | head=50, tail=50 | clean_atom_points on atomsegnet1742_results, pattern `????_pos_gaussian_blur_Gen1-gaussianMask.txt`, pixel_cell=1.0, nn_factor=0.8, min_dist=10, quantile=0.65, min_score=0.4; overlays via overlay_clean_points radius=3 color=red base=/dataset/1742 | Outputs `_pos_gaussian_blur_Gen1-gaussianMask_gaussian_blur_clean.txt` + `_origin_clean_gaussian_blur_Gen1-gaussianMask.png` for first/last 50 |
| UDVD subset inference | head=50, tail=50 (`1742_udvd_incnb_0000–0049 & 1125–1174`) | batch_run_atomsegnet.py input=1742_udvd_subset output=atomsegnet_udvd_results --cuda --overwrite | Adds result folders per denoised JPEG name |
| UDVD sigma14 blur + masks | same subset | blur_dataset.py → `blured_udvd_subset_sigma14` (`σ=14`, normalize); atom_cluster_segment.py → `atom_cluster_out_udvd_sigma14` with same segmentation params as baseline | Provides `{}_heavyblur_mask.png` for new denoise inputs |
| UDVD mask filter & dedup | head=50, tail=50, `--use-mask-names` | apply_cluster_filter.py: mask_root=atom_cluster_out_udvd_sigma14, mask_template=`{}_heavyblur_mask.png`, detection_root=atomsegnet_udvd_results, dir_template=`{}`, points_template=`{}_pos_Gen1-gaussianMask.txt`, dataset_root=1742_udvd_incnb_jpg, dilate=15, radius=3, color=red, suffix=gaussian_blur; clean_atom_points `pixel_cell=1.0`, `nn_factor=0.8`, `min_dist=10`, `quantile=0.65`, `min_score=0.4`; overlay_clean_points radius=3 color=red `--base-name-mode full` | Outputs `_pos_gaussian_blur_Gen1-gaussianMask*.txt` + `_origin_{gaussian_blur,clean}_Gen1-gaussianMask.png` under atomsegnet_udvd_results |
| UDVD full-set pipeline | All 1 175 images | `batch_run_atomsegnet.py 1742_udvd_incnb_jpg atomsegnet_udvd_results_full --cuda`; `blur_dataset.py --sigma 14 --normalize`; `atom_cluster_segment.py` → `atom_cluster_out_udvd_sigma14_full`; `apply_cluster_filter.py --use-mask-names --dilate 15` with dataset-root=1742_udvd_incnb_jpg; `clean_atom_points` (`pixel_cell=1.0`, `nn_factor=0.8`, `min_dist=10`, `quantile=0.65`, `min_score=0.4`); `overlay_clean_points --base-name-mode full` | Produces `_pos_gaussian_blur_Gen1-gaussianMask{,_gaussian_blur_clean}.txt`, `_origin_gaussian_blur_*.png`, `_origin_clean_Gen1-gaussianMask.png` for every `1742_udvd_incnb_xxxx` folder |
